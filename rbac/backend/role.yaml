apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: backend-rbac
  namespace: bulletin
rules:
- apiGroups: [""]
  resources:
    - configmaps
    - secrets
  verbs:
    - get
    - list
