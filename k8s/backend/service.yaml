apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: bulletin
spec:
  type: ClusterIP

  selector:
    app: backend
  ports:
    - name: http-backend
      port: 8080
      targetPort: 8080

    - name: management
      port: 9090
      targetPort: 9090
