apiVersion: v1
kind: ResourceQuota

metadata:
  name: bulletin-resource-quota
  namespace: bulletin

spec:
  hard:
    requests.cpu: "2"
    limits.cpu: "5"

    requests.memory: 2Gi
    limits.memory: 5Gi
